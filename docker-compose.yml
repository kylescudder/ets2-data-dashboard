services:
  db:
    image: timescale/timescaledb:latest-pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ets2
      POSTGRES_PASSWORD: ets2
      POSTGRES_DB: ets2
    ports:
      - "5432:5432"
    volumes:
      - ets2_pg:/var/lib/postgresql/data

volumes:
  ets2_pg:
